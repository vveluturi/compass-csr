{
  "buildCommand": "cd compass-csr && npm install && npm run build",
  "outputDirectory": "compass-csr/dist",
  "rewrites": [
    { "source": "/api/anthropic/v1/messages", "destination": "/api/anthropic" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
